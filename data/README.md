# data/ (local only)

This is the only tracked file in `data/`. Everything else here is gitignored: the repo is public and the course data is not ours to redistribute.

Expected local layout (see [`../docs/DATA.md`](../docs/DATA.md)):

```
data/
├── raw/fordham/   # homework 1-min archive, as received
├── raw/tick/      # tick delivery, as received
└── hdb/           # built by q/load/ from raw/
```
