# data/ (local only)

This is the only tracked file in `data/`. Everything else here is gitignored

Expected local layout (see [`../docs/DATA.md`](../docs/DATA.md)):

```
data/
├── raw/fordham/   # homework 1-min archive
├── raw/tick/      # tick delivery
└── hdb/           # built by q/load/ from raw/
```
